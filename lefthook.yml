pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.{js,ts,tsx}"
      run: bunx oxlint --fix {staged_files}
      stage_fixed: true
    - name: format
      run: bunx oxfmt {staged_files}
      stage_fixed: true
